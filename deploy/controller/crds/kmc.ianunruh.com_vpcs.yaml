---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: vpcs.kmc.ianunruh.com
spec:
  group: kmc.ianunruh.com
  names:
    kind: VPC
    listKind: VPCList
    plural: vpcs
    shortNames:
    - vpc
    singular: vpc
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.vlan
      name: VLAN
      type: integer
    - jsonPath: .spec.cidr
      name: CIDR
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.vlanPoolRef.name
      name: Pool
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          VPC is a namespaced self-service private network (Multus bridge + VLAN).
          Object name is the Multus NetworkAttachmentDefinition name tenants attach VMs to.

          Router attachment is intentionally out of band: a future Router CR will set
          status.routerRef / annotation kmc.ianunruh.com/router. Reserved GVK for that
          work: routers.kmc.ianunruh.com (kind Router, shortName rtr).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              VPCSpec defines the desired state of a self-service private network.
              One optional private CIDR (subnet) per VPC — matches the console product model.
            properties:
              cidr:
                description: Optional private IPv4 CIDR for IPAM on this VPC (e.g.
                  10.40.1.0/24).
                type: string
              description:
                description: Human-readable description.
                type: string
              dns:
                description: Optional DNS resolvers for guests on this VPC.
                items:
                  type: string
                type: array
              gateway:
                description: Optional default gateway inside the CIDR (often the future
                  router interface IP).
                type: string
              vlanPoolRef:
                description: VLANPoolRef names a cluster-scoped VLANPool to allocate
                  a VLAN from.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - vlanPoolRef
            type: object
          status:
            description: VPCStatus defines the observed state of VPC.
            properties:
              bridge:
                description: Bridge copied from the VLANPool at assignment time.
                type: string
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              networkAttachmentReady:
                description: NetworkAttachmentReady is true when the owned Multus
                  NAD exists and matches.
                type: boolean
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation last processed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: 'Phase is a high-level summary: Pending, Ready, or Error.'
                type: string
              routerRef:
                description: |-
                  RouterRef is set by a future Router controller when a shared router attaches
                  to this VPC (mirrors annotation kmc.ianunruh.com/router). Reserved; not
                  written by the VPC controller today.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              vlan:
                description: VLAN assigned from the VLANPool (immutable once set).
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
