---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: portforwards.kmc.ianunruh.com
spec:
  group: kmc.ianunruh.com
  names:
    kind: PortForward
    listKind: PortForwardList
    plural: portforwards
    shortNames:
    - pf
    singular: portforward
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.publicAddress
      name: Public
      type: string
    - jsonPath: .spec.publicPort
      name: Port
      type: integer
    - jsonPath: .spec.protocol
      name: Protocol
      type: string
    - jsonPath: .spec.privateAddress
      name: Private
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          PortForward is a namespaced port DNAT rule (publicIP:port → privateIP:port).
          The PortForward controller validates; the Router controller projects DNAT
          into policy. Ready is true when the agent is Ready.

          A public address must not be both a full FloatingIP association and a
          PortForward host — the Router render prefers the FloatingIP association.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              PortForwardSpec defines a port-level DNAT rule through a router external gateway.
              Distinct from FloatingIP (full 1:1): multiple PortForwards may share one public address.
              Realization is done by the Router controller (policy projection + agent).
            properties:
              privateAddress:
                description: Private target address in the VPC (no prefix).
                minLength: 1
                type: string
              privatePort:
                description: Private target port.
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              protocol:
                description: 'Protocol: TCP or UDP.'
                enum:
                - TCP
                - UDP
                type: string
              publicAddress:
                description: Public listen address (no prefix).
                minLength: 1
                type: string
              publicPort:
                description: Public listen port.
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              routerRef:
                description: Router that should program DNAT. When empty, the Router
                  attaching the VPC is used.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              targetVM:
                description: Optional target VirtualMachine (same namespace).
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              vpcRef:
                description: VPC that owns the private address (same namespace).
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - privateAddress
            - privatePort
            - protocol
            - publicAddress
            - publicPort
            - vpcRef
            type: object
          status:
            description: PortForwardStatus defines the observed state of PortForward.
            properties:
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation last processed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: 'Phase: Pending, Ready, or Error.'
                type: string
              programmed:
                description: Programmed is true when a Router has projected this rule
                  into policy.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
