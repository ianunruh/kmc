---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: floatingips.kmc.ianunruh.com
spec:
  group: kmc.ianunruh.com
  names:
    kind: FloatingIP
    listKind: FloatingIPList
    plural: floatingips
    shortNames:
    - fip
    singular: floatingip
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.address
      name: Address
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.vpcRef.name
      name: VPC
      type: string
    - jsonPath: .spec.privateAddress
      name: Private
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          FloatingIP is a namespaced public floating IPv4 for a VPC (hold or associate).
          Recommended object name: public address with dots → dashes (10-20-30-40).

          Until the Router CR exists, the controller only validates and sets Held/Associated
          from the spec; Ready remains false (RouterNotImplemented).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              FloatingIPSpec defines a public floating address for a VPC.
              Realization (SNAT/DNAT on the appliance) requires a future Router controller.
            properties:
              address:
                description: Optional preferred public IPv4 address. Empty means allocate
                  (via IPAddress claim later).
                type: string
              poolRef:
                description: PoolRef identifies the public IPv4 pool (typically kind
                  IPPool).
                properties:
                  kind:
                    description: 'Kind of pool. Allowed: "VPC", "IPPool".'
                    enum:
                    - VPC
                    - IPPool
                    minLength: 1
                    type: string
                  name:
                    description: Name of the pool resource (namespaced for VPC; cluster-scoped
                      for IPPool).
                    minLength: 1
                    type: string
                required:
                - kind
                - name
                type: object
              privateAddress:
                description: Private VPC address when associated. Empty means held
                  (public reserved, unmapped).
                type: string
              routerRef:
                description: |-
                  Router that should program SNAT/DNAT. Reserved for the Router CR; Ready
                  stays false until a Router controller exists and sets status.programmed.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              targetVM:
                description: Optional target VirtualMachine (same namespace) for the
                  private address.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              vpcRef:
                description: VPC this floating IP belongs to (same namespace).
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - poolRef
            - vpcRef
            type: object
          status:
            description: FloatingIPStatus defines the observed state of FloatingIP.
            properties:
              address:
                description: Allocated public address (dotted-quad).
                type: string
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation last processed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: 'Phase: Pending, Held, Associated, or Error.'
                type: string
              prefixLength:
                description: Prefix length of the public address on the external Multus
                  NIC.
                format: int32
                type: integer
              programmed:
                description: Programmed is true when a future Router controller has
                  applied this mapping.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
