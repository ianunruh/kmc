/** Server re-export of shared membership helpers (import path convenience). */
export {
  groupMembership,
  labelsMatchSelector,
  labelsMembership,
  membershipAnnotations,
  membershipFromServiceMeta,
  membershipLabels,
  parseMatchLabelsText,
  resolveServiceSelector,
  singleVmMembership,
} from "./membership";
